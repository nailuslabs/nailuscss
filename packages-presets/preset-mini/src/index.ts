import type { NailusConfig } from '@nailuscss/core';

export const presetMini: NailusConfig = {
  theme: {
    colors: {
      // Vos couleurs
    },
    spacing: {
      // Vos espacements
    },
  },
  rules: [],
  variants: [],
};

export default presetMini;